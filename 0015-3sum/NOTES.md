```
Sort the input array in ascending order
While looping through nums use a two-pointer approach to iterate through the array agian, checking the three numbers that add up to zero. Keep track of duplicates using a set.
```